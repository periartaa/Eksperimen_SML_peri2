name: iris_classification_workflow

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      input_path:
        type: string
        default: "iris_processed.csv"
    command: "python modelling.py {input_path}"