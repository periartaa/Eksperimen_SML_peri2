name: Iris-Classification-CI

parameters:
  input_data:
    type: str
    default: "namadataset_preprocessing/iris_processed.csv"

entry_points:
  main:
    parameters:
      input_data: {type: str, default: "namadataset_preprocessing/iris_processed.csv"}
    command: "python modelling.py --input_data {input_data}"